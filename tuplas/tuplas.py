frutas = ("laranja", "banana", "laranja", "uva", "laranja", )

letras = tuple("python")

numeros = tuple([1,2,3,4])

pais = ("Brasil",)

# Acesso direto | igual a listas

print(frutas[0])

# Matriz

matriz = (
    [1, 2, 3],
    [4, 5, 6],
    [3, 6, 9],
)

# Métodos

# count

print(frutas.count("laranja"))

# len

print(len(frutas))